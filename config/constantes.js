var constantes = {
   
    DELAY_WAKE_UP_HEROKU_APP = 1000 * 60 * 20, // delay in milliseconds - 20 minutes
    DELAY_GET_WAITING_TIMES = 1000 * 10 * 1, // delay in milliseconds to get waiting times
   
    OPENING_TIMES = 'OPENING_TIMES',
    LAST_WAITING_TIMES = 'LAST_WAITING_TIMES'    
};

module.exports = constantes